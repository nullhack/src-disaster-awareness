[project]
name = "src-disaster-awareness"
version = "0.1.0"
description = "Disaster Awareness Agent - Monitoring and data pipeline"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fire>=0.5.0",
]

[project.scripts]
disaster-monitor = "disaster_monitor.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "ruff>=0.1.0",
]
