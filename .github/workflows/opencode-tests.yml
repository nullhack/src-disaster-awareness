name: OpenCode Tests

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/anomalyco/opencode:latest

    steps:
      - uses: actions/checkout@v4

      - name: Run OpenCode Test
        run: |
          opencode run 'just say "It is working"'
